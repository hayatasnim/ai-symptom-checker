Draw.io Code for Activity Diagram (4.3)To make section 4.3 complete, Ho can paste this code directly into draw.io (Arrange $\rightarrow$ Insert $\rightarrow$ Advanced $\rightarrow$ Mermaid) to generate the diagram image automatically:

CODE SNIPPETS: 

flowchart TD
    Start([Start: Open SymptoCare App]) --> Nav[Navigate to 'Check Symptoms' Tab]
    Nav --> Input[Select Observed Symptoms]
    Input --> Click[Click 'Analyze' Button]
    Click --> Decision{Are symptoms selected?}
    
    Decision -- No (0 symptoms) --> Alert[Display Alert: 'Please select at least one symptom before analyzing']
    Alert --> Input
    
    Decision -- Yes (1 or more) --> Vector[Build Binary Feature Array]
    Vector --> ML[Execute Scikit-Learn ML Model Inference]
    ML --> Output[Display Primary Assessment & Possibilities Breakdown]
    Output --> End([End])

If Ho uses draw.io:Go to draw.io.Click Arrange $\rightarrow$ Insert $\rightarrow$ Advanced $\rightarrow$ Mermaid.Paste this code directly to generate the diagram automatically: (DIAGRAM 4.1)

CODE SNIPPETS:

graph LR
    subgraph SymptoCare Web Application
        UC1((Select Symptoms))
        UC2((Enter Patient Demographics))
        UC3((Trigger AI Analysis))
        UC4((View Primary Assessment))
        UC5((Browse Informational Pages))
    end

    User[User / Patient] --> UC1
    User --> UC2
    User --> UC3
    User --> UC4
    User --> UC5

    UC3 --> AI[AI Prediction Engine]
    UC4 --> AI


FIGURE 4.3
 CODE SINPPETS:
flowchart LR
    A[User Inputs: Multi-Select Symptoms] --> B[Binary Vectorization: 0/1 DataFrame]
    B --> C[Loaded .pkl Model Artifacts]
    C --> D[Prediction Output & Probabilities]

Once generated, export it as a PNG image and paste it directly under Figure 4.3 in Section 4.4

